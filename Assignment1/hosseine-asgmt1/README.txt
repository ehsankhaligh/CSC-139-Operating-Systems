Compile the code using gcc:

gcc observer.c -o observer

To run each program:

./observer

Standard   observer

./observer -s

Short       observer -s

./observer [input interval] [input duration]

Long       observer -l [input interval] [input duration]